{
  "name": "tt4-2930-be",
  "version": "1.0.0",
  "description": "test",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "run": "npm run dev",
    "build:backend": "rm -rf dist && mkdir -p dist/src && cp -R src/* dist/src/ && cp package.json package-lock.json dist/",
    "build:frontend": "npm --prefix frontend install && npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend && mv frontend/dist/frontend dist/public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.23.0"
  }
}
